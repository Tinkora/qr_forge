# 安全策略

[English](./SECURITY.md)

## 支持的版本

| 版本 | 支持状态 |
| ------ | ---------- |
| 最新 GitHub release | 支持 |
| `main` | 接收用于下一版本的修复 |
| 更旧版本 | 除非安全公告明确列出，否则不支持 |

qr_forge 由小型团队维护。安全修复会被优先处理，但不提供服务等级响应或 backport 保证。

## 私密报告漏洞

不要为疑似漏洞创建公开 issue、discussion 或 PR。

[创建私密 security advisory](https://github.com/Tinkora/qr_forge/security/advisories/new)

GitHub 私密漏洞报告是唯一正式安全渠道。报告应包含：

- 受影响的版本、commit、URL 或浏览器
- 复现步骤或最小 proof of concept
- 预期行为和实际行为
- 安全影响及所需用户交互
- 建议的缓解措施或披露限制

维护者争取在五个工作日内确认信息完整的报告。调查时间取决于可复现性、严重程度和维护者时间。报告者会通过私密 advisory 收到状态更新。

## 范围内问题

- 由于应用行为导致用户输入离开浏览器
- 通过生成的 SVG、预览、文件名或状态消息注入脚本或 markup
- 不安全地解析 PNG Logo 或生成的图像数据
- 绕过已记录的输入和资源限制
- 可归因于本项目的 WebAssembly 内存安全或沙箱边界问题
- 对 qr_forge 造成实际影响的依赖漏洞
- GitHub Actions、发布产物或 Pages 部署的供应链问题

## 通常不在范围内的问题

- 不影响 qr_forge 的浏览器、扫描器或依赖问题
- 托管服务商针对普通静态资源请求产生的日志
- 社会工程、物理攻击或针对 GitHub 的拒绝服务
- 只有自动扫描器输出、没有可复现影响的报告
- 不受支持的 fork 或修改后部署

维护者仍可能协助将可信问题报告给上游。

## 安全与隐私模型

qr_forge 是静态浏览器应用。二维码和条形码生成通过页面内的 WebAssembly 运行。应用没有账号系统、数据库、analytics 或生成 API，也不会主动传输用户输入的 payload。

这个边界不代表整个浏览会话都是匿名的：

- GitHub Pages 或其他托管服务会收到 HTML、CSS、JavaScript、WebAssembly 等静态资源的普通请求及相关元数据。
- 浏览器扩展、受侵害的浏览器或被修改的部署可能观察页面内容。
- 下载的 SVG 和 PNG 文件包含已编码 payload，必须根据其敏感程度妥善处理。
- 带 Logo 的二维码无法保证在所有实际环境下都能扫描；关键输出应由独立扫描器验证。

生成的 SVG 使用项目创建的几何元素，不插入用户提供的 markup。PNG Logo 在本地解码，并同时受到浏览器 UI 限制和核心校验约束。

## 协调披露

维护者会验证报告、评估受影响版本、准备修复与测试，并通过私密 advisory 协调发布。公开披露应等待修复或双方同意的缓解措施就绪。是否署名由报告者决定。
